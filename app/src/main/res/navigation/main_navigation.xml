<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_navigation"
    app:startDestination="@+id/navigation_saved_notes">

    <fragment
        android:id="@+id/navigation_saved_notes"
        android:name="com.duzce.spotinotes.ui.SavedNotes"
        android:label="@string/saved_notes"/>

    <fragment
        android:id="@+id/navigation_profile"
        android:name="com.duzce.spotinotes.ui.Profile"
        android:label="@string/profile"/>

</navigation>